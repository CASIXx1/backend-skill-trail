{
  "family": "{{ tfstate `output.worker_ecs_service_name` }}",
  "taskRoleArn": "{{ tfstate `output.worker_ecs_task_role_arn` }}",
  "executionRoleArn": "{{ tfstate `output.ecs_task_execution_role_arn` }}",
  "networkMode": "awsvpc",
  "containerDefinitions": [
    {
      "name": "{{ must_env `WORKER_CONTAINER_NAME` }}",
      "image": "{{ tfstate `output.worker_ecr_repository_url` }}:{{ must_env `IMAGE_TAG` }}",
      "essential": true,
      "stopTimeout": 30,
      "environment": [
        {
          "name": "AWS_REGION",
          "value": "{{ must_env `AWS_REGION` }}"
        },
        {
          "name": "WORKER_QUEUE_URL",
          "value": "{{ tfstate `output.worker_queue_url` }}"
        },
        {
          "name": "WORKER_WAIT_TIME_SECONDS",
          "value": "{{ env `WORKER_WAIT_TIME_SECONDS` `20` }}"
        },
        {
          "name": "WORKER_MAX_MESSAGES",
          "value": "{{ env `WORKER_MAX_MESSAGES` `10` }}"
        },
        {
          "name": "WORKER_VISIBILITY_TIMEOUT",
          "value": "{{ env `WORKER_VISIBILITY_TIMEOUT` `30` }}"
        },
        {
          "name": "DB_HOST",
          "value": "{{ tfstate `output.database_writer_endpoint` }}"
        },
        {
          "name": "DB_READER_HOST",
          "value": "{{ tfstate `output.database_reader_endpoint` }}"
        },
        {
          "name": "DB_PORT",
          "value": "{{ tfstate `output.database_port` }}"
        },
        {
          "name": "DB_NAME",
          "value": "{{ tfstate `output.database_name` }}"
        },
        {
          "name": "CACHE_HOST",
          "value": "{{ tfstate `output.cache_primary_endpoint` }}"
        },
        {
          "name": "CACHE_PORT",
          "value": "{{ tfstate `output.cache_port` }}"
        },
        {
          "name": "CACHE_TLS_ENABLED",
          "value": "{{ env `CACHE_TLS_ENABLED` `true` }}"
        }
      ],
      "secrets": [
        {
          "name": "DB_USER",
          "valueFrom": "{{ tfstate `output.database_app_user_secret_arn` }}:username::"
        },
        {
          "name": "DB_PASSWORD",
          "valueFrom": "{{ tfstate `output.database_app_user_secret_arn` }}:password::"
        },
        {
          "name": "CACHE_AUTH_TOKEN",
          "valueFrom": "{{ tfstate `output.cache_auth_token_secret_arn` }}:AUTH_TOKEN::"
        }
      ],
      "logConfiguration": {
        "logDriver": "awsfirelens",
        "options": {
          "Name": "newrelic"
        },
        "secretOptions": [
          {
            "name": "apiKey",
            "valueFrom": "{{ tfstate `output.external_service_secret_arn` }}:NEW_RELIC_LICENSE_KEY::"
          }
        ]
      }
    },
    {
      "name": "log_router",
      "image": "{{ tfstate `output.new_relic_firelens_image` }}",
      "essential": true,
      "firelensConfiguration": {
        "type": "fluentbit",
        "options": {
          "enable-ecs-log-metadata": "true"
        }
      },
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "{{ tfstate `output.worker_log_group_name` }}",
          "awslogs-region": "{{ must_env `AWS_REGION` }}",
          "awslogs-stream-prefix": "firelens"
        }
      }
    }
  ],
  "requiresCompatibilities": [
    "FARGATE"
  ],
  "cpu": "{{ env `WORKER_TASK_CPU` `256` }}",
  "memory": "{{ env `WORKER_TASK_MEMORY` `512` }}",
  "runtimePlatform": {
    "cpuArchitecture": "ARM64",
    "operatingSystemFamily": "LINUX"
  }
}
