region: '{{ must_env `AWS_REGION` }}'
cluster: '{{ must_env `MIGRATION_ECS_CLUSTER_NAME` }}'
service: '{{ must_env `MIGRATION_CONTAINER_NAME` }}'
service_definition: run-def.json
task_definition: task-def.json
timeout: 10m
plugins:
  - name: tfstate
    config:
      url: '{{ must_env `TFSTATE_URL` }}'
